# Copie este arquivo para `.env` e preencha com os valores necessarios.
# Nao comite segredos no git.

# Envio do resultado do Quiz V1 (default já aponta para o endpoint do n8n)
VITE_QUIZ_V1_WEBHOOK_URL=

